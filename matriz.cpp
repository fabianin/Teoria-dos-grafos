#include <bits/stdc++.h>
#define V 7
using namespace std;


int main(){
    int maj[V][V];
    vector<string> comites;
    string str;
    int h, c,m;
    cin >> c >> m >> h;
    int k[m][c];
    for(int i=0; i<V;i++){
        cin >> str;
        comites.push_back(str);
        cout << str;
        for(i=0;i<str.size();i++){

        }
    }
}
